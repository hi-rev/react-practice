import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';

// 리액트가 렌더링 할 컴포넌트를 'root' 요소에 렌더링한다.
const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<App />);